find_st_path(node start , node output){
	//when node output reached reverse 
	visit_Next_node();
	while(currNode!=output){
		//find loops

		if(//findloops){
			currNode==stk[stk.count()-1)];
		}
	}
	string temp="";
	//Get all node names add in string array

}

find_All_St_paths(){
	while(stk.Any(node.visited==false)){
		find_st_path();
	}
}